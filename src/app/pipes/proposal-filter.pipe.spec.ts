import { ProposalFilterPipe } from './proposal-filter.pipe';

describe('ProposalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProposalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
